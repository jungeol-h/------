# docs/ — 기획·운영 자료

**코드 작업에 필요한 문서는 여기가 아니라 각 코드 폴더의 README에 있다** (루트 README.md의
코드 지도 참고). 이 폴더는 클라이언트 원본 자료와 운영 파일 보관용이다.

| 폴더 | 내용 | 신뢰도 |
|---|---|---|
| `1. Task/` | 현행 작업 문서. `6. UX 개선 계획*.md`은 **최신·신뢰 가능** (미구현 계획) | 현행 |
| `2. 자료/` | 운영 자료 — 계정목록.csv, 교재 PDF, 시연 영상, 출석부 | 운영 참고 |
| `99. 보관됨/` | 반영 완료된 기획 원본 (아래) | **역사 기록 — 현행 아님** |

## 보관된 것들 (근거 추적이 필요할 때만)

- `99. 보관됨/기획 원본(2026-07 반영완료)/` — 클라이언트 요청 1~5번(전부 구현됨),
  `우선 순위 상세 정리.hwpx`(구현됨), `개념 정리/`(도메인 개념 원본 — 핵심 수치는
  `src/platform/context/selectors/README.md`로 이관됨), 시스템 개요·비전.
- `99. 보관됨/산청 우정학사 중등부 베타테스트/` — 베타 초기 요청·계정 자료.
- `99. 보관됨/과거 쪽지시험 알파테스트 레포(quiz-app)/` — 별개 과거 프로젝트 전체
  (git 추적 안 함).

주의: 보관 문서 속 수치·탭 구성은 이후 바뀌었을 수 있다. 현행 기준은 항상 코드와
코드 폴더 README.
